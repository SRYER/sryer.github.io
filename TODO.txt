
- Analytics-konto

- Js artikel:
	- Fix position af stats.js vindue
	- Cache static-værdier i stedet for at beregne ved hvert refresh
	- Cache statisk lyskilde for at vise performance

- VM med photoshop
- Pladespilleren
- SQL Connection Manager
- SQL Custom typer
- What I have learned as a CTO
- Opdagelsen af code golf: https://codegolf.stackexchange.com/questions/205638/dont-do-not-lowercase-uppercase-this
- Why you should never ask other people if there are gonna have kids
- AI til at lave musik
- Hell March Live-video: https://www.youtube.com/watch?v=TrqQxl5o3eg
- Guitar Shred

- Skriv om sql finding:


DECLARE @custId UNIQUEIDENTIFIER = '7d9f53e6-7ece-455f-94dd-fda82400e04d'

DECLARE @limit INT = 10
DECLARE @customerIdTable dbo.guidlist
INSERT INTO @customerIdTable(VALUE) VALUES('7d9f53e6-7ece-455f-94dd-fda82400e04d')
DECLARE @scheduledExecutionStart DATETIME = '2020-01-16'
DECLARE @scheduledExecutionEnd DATETIME = '2020-06-30'

DECLARE @minSummaryId bigint = -1
DECLARE @maxSummaryId bigint = -1

SELECT TOP (@limit) *
FROM OutgoingMessages WITH(NOLOCK, INDEX(IX_OutgoingMessages_CustomerId_ScheduledExecutionDate))
JOIN @customerIdTable CustomerIds ON OutgoingMessages.CustomerId = CustomerIds.Value
WHERE ScheduledExecutionDate >= @scheduledExecutionStart AND ScheduledExecutionDate < @scheduledExecutionEnd
ORDER BY ScheduledExecutionDate DESC

SELECT TOP (@limit) *
FROM OutgoingMessages WITH(NOLOCK, INDEX(IX_OutgoingMessages_CustomerId_ScheduledExecutionDate))
WHERE ScheduledExecutionDate >= @scheduledExecutionStart AND ScheduledExecutionDate < @scheduledExecutionEnd
and customerId = @custId
ORDER BY ScheduledExecutionDate DESC
